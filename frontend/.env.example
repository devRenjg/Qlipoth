# 前端本地环境变量示例。复制为 .env.local 并填入真实值(.env.local 不进git)。
# 内部审批单据域名(仅管理员校准报备用),留空则不显示该链接。
VITE_SHENPI_URL=

# 正式域名 IP 直连拦截。两者都留空则不启用拦截(放行所有 Host,适合本地/开源部署)。
# 生产部署时填入正式入口域名，非正式域名访问会被引导跳转。
VITE_OFFICIAL_HOST=
VITE_OFFICIAL_URL=
