# 克里珀 - 大型活动保障知识库技术方案文档

> 当前版本：20260615.3  
> 最后更新：2026-06-15

---

## 版本历史

| 版本号 | 日期 | 变更内容 | 作者 |
|--------|------|----------|------|
| 20260615.3 | 2026-06-15 | 作战地图全量化(质量优先)：去掉每方向80篇上限改为全量扫描(高可用402篇全看，此前漏322篇)，并修复要点汇总阶段的截断隐患→改"分层归并"(要点过多时先分批LLM压缩去重再汇总，不丢关键信息)，并发4→6。6方向全量重生成共覆盖1009篇次，内容完整度显著提升(如高可用归纳出决赛黑屏事故负载均衡瞬时74.9W、6.3万QPS超4万quota触发P0限流等具体细节)。新增CLAUDE.md记录协作价值取向(质量优先/诚实第一/微信优先/先对齐) | Claude + User |：帮新保障负责人"知道自己不知道"。独立导航页，基于知识库全部文档+主题标签方向用LLM归纳6个核心方向(事故故障/高可用/直播体验/成本/安全/业务需求)的认知卡片(定位/关键系统/历史事故/水深点/推荐文档)，与保障清单完全解耦(数据源是文档非清单条目)。两段式生成(并发抽要点→汇总卡片)避免上百篇全文一次喂LLM；新增battle_map表后台生成+缓存，全体只读、管理员可重新生成、进度可轮询。首版6方向已生成(各65-80篇)，内容基于真实复盘归纳。配套：同一用户每活动仅一份清单(重复生成409+下拉隐藏已生成活动) | Claude + User |
| 20260615.1 | 2026-06-15 | OpenSpec 整体归档与 specs 合并：将 15 个已完成 change 的 spec delta 合并进正式 specs/ 并归档到 changes/archive/。重点新建 specs/battle-checklist（保障清单核心能力，此前散落在7个change从未合并）含历史踩坑清单生成(六维度+事故优先+跨活动事故必借+语义查重+数据并入)/结构化字段与排序(day_late)/查看维护/生成者标注/写操作仅本人/导出企微/作战地图。并更新 agentic-search(摘录密度优先+关键词健壮性)、online-doc-import(企微API读写+图片落地+批量重导+LLM表格摘要+每日评测+Confluence)、auth-and-permissions(文档导入仅管理员+用户行为日志)、document-rendering(新旧版本对比)、document-tagging(活动维度标签+smartsheet)。纯文档整理不改代码 | Claude + User |
| 20260613.2 | 2026-06-13 | 用户行为日志：新增user_activity表+activity.py埋点模块，记录主要功能使用(问答/生成清单/导出清单)，埋点"尽力而为"不影响主功能；用户管理页每用户加「行为日志」抽屉(各功能次数统计+时间倒序明细)，查询接口仅管理员；从chat_history(53条问答)+checklists回填存量历史(保留原时间)；放宽重导替换门槛(读成功无明显倒退即替换，20配额≈20篇入库) | Claude + User |
| 20260613.1 | 2026-06-13 | 检索关键词健壮性修复：每日重导评测中发现 LLM 生成的搜索关键词偶发返回 dict/嵌套list，导致 grep_search 调 .strip() 崩溃(线上问答隐患)。在检索入口统一强制关键词为非空字符串(dict取keyword字段/list拼接/其余str兜底)，根治崩溃。每日重导首日导入13篇(累计47/745)，录入质量旧→新:图片0→124、乱码645→12、表格行3→1457；检索效率新库11.7s vs 旧库15.3s(快24%) | Claude + User |
| 20260612.3 | 2026-06-12 | 表格检索优化(打通竖排表数据问答)：①LLM表格行级摘要——导入时用LLM理解表格(含企微竖排碎片乱表)生成"字段＝值"行摘要追加到专用块，规则法只能处理横排表、LLM能还原竖排乱表，存量API文档全部补做、每日导入自动apply；②检索摘录密度优先——命中行过多时按"覆盖的不同关键词数"降序优先纳入上下文(表格摘要行先进)，"摘要"块识别为高优先级section，根治"目标行在文档末尾被预算挤掉"；③A/B因果对照(同数据同题切开关)实测：竖排表数据题能答出5/10→10/10、6胜0负4平无回归，老痛点题(iOSImage/码率/qps)从答不出→答对；④每日重导间隔10s+记录当日清单，新增eval_daily当日新增文档自动评测；⑤导入历史删除收紧为仅管理员 | Claude + User |
| 20260612.2 | 2026-06-12 | 协作归属与权限收紧：①保障清单标注生成者(checklists.created_by，卡片/详情展示)，现有3份手动标注；②清单写操作归属隔离——删除/增删改条目/勾选/导出企微 仅生成者本人可操作，admin也不例外，他人只读，前端隐藏写控件显示"仅查看"；③文档导入/删除收紧为仅管理员，并修复越权漏洞(upload系列接口、DELETE /documents/{id} 原本无鉴权任何人可调)，非管理员上传页只展示导入历史、文档页可读但不能删/打标签；④检索表格防截断(命中表格行纳入整张表)+企微竖排碎片表格保守合并；⑤春晚清单#10#11补刷"当天发生·务必重视"标记+排序 | Claude + User |
| 20260612.1 | 2026-06-12 | 企微 API 接入 + 清单导出 + 知识库重导：①新增 wecom.py 封装企微官方 CLI(读写在线文档，base64图片落地本地，node直调绕开.cmd坑，限流退避)；②知识库批量重导 migrate_wecom_reimport.py——企微API替换Playwright旧抓取，逐篇质量门禁(正文/图片/表格/乱码对比)判定better才替换，旧版备份knowledge_base_old可回滚+新旧对比；实测代际提升(图片0→全有、表格乱码→完整、噪声清98%)；③保障清单导出企微在线文档(选择/全选→Markdown复选框Checklist，超长自动分文档)；④清单质量增强：事故维度审计降级(P0收敛)、LLM语义查重(同事件全清单只留一条)、day_late"当天发生·务必重视"标记+优先排序；⑤Confluence(info)递归导入 confluence.py + 图片cookie代理；⑥文档页"查看新/查看旧"对比+列表自动刷新；⑦修复内网HTTP下crypto.randomUUID致问答无响应 | Claude + User |
| 20260609.2 | 2026-06-10 | 清单事故判定标准升级与体检：①事故/故障维度判定优先级最高——重大影响/资损无论起因(攻击/bug/容量/依赖)都归事故+P0，安全维度收窄为"未酿成事故的风险点"；②跨活动借鉴新规则：事故/故障条目必借(绕过通用性判断和每维限额)；③跨晚标签严判修正(253→50，去掉误打的CNY春晚内容)；④春晚清单体检补severity+归位真示例故障(若干条→事故/P0)；⑤S赛清单重生成#7(232条，事故29/P0 20，示例故障已进P0)+审计/修正运维脚本 | Claude + User |
| 20260608.5 | 2026-06-08 | 保障清单归属字段修复：原team/owner全空，根因是复盘负责人多为脱敏ID且抽取没用文档标题。修复后 team 改判"负责团队/业务线"(流媒体/基架/弹幕/OTT/产品等，109条全判出)，owner 只保留真实中文姓名(脱敏ID留空手动补)。现有清单回填不动正文勾选 | Claude + User |
| 20260608.4 | 2026-06-08 | 保障清单增强：六维度可折叠、勾选完成可追溯(记录人+时间)、按全部/未完成/已完成筛选、维度内按保障周期阶段(备战前期→压测演练→上线前→活动当天→活动后)排序、标注团队/负责人(可留空手动补)。现有S赛清单回填阶段不重新生成 | Claude + User |
| 20260608.3 | 2026-06-08 | 保障清单(历史踩坑预警)：知识库升级为作战记忆体的第一步。选活动(S赛/跨晚/春晚)→扫历史复盘→LLM按保障六维度提炼踩坑(现象/原因/处置/建议/时点/来源)→落库成可编辑可跟踪的备战清单(独立TAB)。验证：S赛43篇复盘抽出117条踩坑，质量高 | Claude + User |
| 20260608.2 | 2026-06-08 | 活动维度标签：新增 S赛/跨晚/春晚 三个活动标签(与主题标签正交、可多选)，规则+LLM混合全库回填(春晚561/跨晚253/S赛67，94%覆盖)；前端活动标签专属配色+分组置前展示 | Claude + User |
| 20260608.1 | 2026-06-08 | Golden Set 升级至150题(补S15活动期/薄弱标签/薄弱类型，回填tags+era元数据，LLM质检)：当前服务基准 Recall@10 0.873/MRR 0.584；评测增加按活动期/标签分组；新增标签过滤搜索评测(点标签后 Recall 0.86→0.93纯收益)+ 零依赖unittest功能测试(15例覆盖路由/导入历史/BM25/标签过滤) | Claude + User |
| 20260605.5 | 2026-06-05 | 导入历史结果可视（root_title="未知"回退首个成功节点标题；成功/跳过/失败三类计数并列展示，修复一批导入只显示成功数、失败数不可见的问题）+ 失败导入批量重试补齐 | Claude + User |
| 20260605.4 | 2026-06-05 | 问题分类路由（规则分类5类，负责人/方案/歧义类走opus保质量、数量/排查类走sonnet提速）：100题横评完整耗时较全opus -42%、质量4.45≈全opus 4.58、opus调用-69%；快模型留空即零回归可秒级回滚 | Claude + User |
| 20260605.3 | 2026-06-05 | BM25 长度归一参数调优（b=0.75→1.0，针对本库巨型扁平文档抑制词频堆叠偏置）：纯 BM25 Recall@10 0.79→0.85、融合后 0.86→0.87，分类型无回退；零依赖、纯参数、可秒级回滚 | Claude + User |
| 20260605.2 | 2026-06-05 | 图文回答（相关图片画廊，从答案来源文档抽内容图旁路下发，零检索回归）、文档查看器图片渲染、标签全站配色统一 | Claude + User |
| 20260605.1 | 2026-06-05 | 文档分类与标签（手动+自动两阶段打标、按标签筛选/查询过滤）、BM25 旁路重排（零依赖 char-bigram + RRF 融合，Recall@10 0.71→0.83）、流式空补全三级兜底、多用户多轮模拟评测 | Claude + User |
| 20260604.1 | 2026-06-04 | 多轮对话上下文（指代消解 + 历史回灌，旁路不动搜索内核）、OpenSpec 规范驱动开发看板、Markdown 富文本渲染（marked + DOMPurify） | Claude + User |
| 20260603.1 | 2026-06-03 | 搜索排序两波优化（IDF 覆盖度打分）、分词器修复（数字+单位前缀拆分）、离线评测体系（golden set + Recall/MRR）、Claude 模型接入与对比 | Claude + User |
| 20260526.1 | 2026-05-26 | 用户体系、权限分层、ripgrep 搜索引擎、PDF/OCR 支持、聊天历史、UI 重构 | Claude + User |
| 20260522.1 | 2026-05-22 | 企业微信文档链接导入、Playwright 抓取、递归嵌套导入、Sheet 支持、去重机制 | Claude + User |
| 20260520.1 | 2026-05-20 | 人设体系、TAPD 集成、多文件覆盖优化、P0 自测用例 | Claude + User |
| 20260519.2 | 2026-05-19 | 流式输出、多模型兼容、性能分析、搜索优化、UI 重构 | Claude + User |
| 20260519.1 | 2026-05-19 | 初始版本，完成核心架构搭建与基础功能实现 | Claude + User |

---

## 1. 项目概述

克里珀（Qlipoth）是一个基于 Agentic Search 理念的大型活动保障知识库系统。区别于传统 RAG（检索增强生成）方案，采用类似 Claude Code / Codex 的工作方式：文件以纯文本形式存储在磁盘上，查询时通过 LLM 理解用户意图、生成搜索策略，再通过 ripgrep 实时搜索知识库内容，最后由 LLM 整合搜索结果生成自然语言回答。

### 核心理念

- **文件即知识**：上传的文档转为 Markdown 文本存储，保持可读性和可搜索性
- **Agentic Search**：LLM 主动生成搜索策略（关键词、文件过滤），而非被动向量匹配
- **流式响应**：SSE 流式输出，用户 1-2 秒即可看到首字，无需等待完整生成
- **多模型兼容**：支持 DeepSeek、通义千问、智谱、Moonshot、OpenAI、Anthropic 等主流模型
- **零向量数据库依赖**：不需要 embedding 模型、向量数据库等额外基础设施
- **在线文档直接导入**：支持企业微信文档/腾讯文档链接导入，Playwright 无头浏览器自动抓取
- **权限隔离**：三级角色体系（管理员/超级用户/普通用户），不同角色不同功能权限
- **多轮对话**：追问时自动指代消解 + 历史回灌，理解"那市场侧呢"式省略，全程旁路不影响单问召回
- **规范驱动开发**：OpenSpec 看板沉淀能力规范（specs）与变更提案（changes），代码与文档同源演进

---

## 2. 技术架构

### 2.1 技术栈

| 层级 | 技术选型 |
|------|----------|
| 前端 | Vue 3 + Vite + Element Plus + Vue Router |
| 后端 | Python 3.12 + FastAPI + Uvicorn |
| 存储 | 本地文件系统（Markdown）+ SQLite（元数据/用户/历史） |
| 搜索 | ripgrep（JSON 模式）+ 关键词扩展 + IDF 覆盖度排序 + Python fallback |
| LLM | OpenAI Chat Completions API / Anthropic Messages API（双格式兼容） |
| 文档解析 | python-docx / python-calamine / python-pptx / pypdf + Tesseract OCR |
| 在线文档抓取 | Playwright（Chromium）+ 网络拦截 + protobuf 解码 |
| 流式传输 | Server-Sent Events (SSE) |
| 认证 | Cookie Token + PBKDF2 密码哈希 + 角色权限 |
| 富文本渲染 | marked（Markdown → HTML）+ DOMPurify（XSS 净化 + 防反向标签劫持） |
| 规范管理 | OpenSpec（specs 能力规范 + changes 变更提案，CLI 归档生成 spec） |

### 2.2 系统架构

```
用户登录（用户名+密码）
  → Cookie Token 自动登录（7天有效）
  → 角色权限校验

用户提问
  → Step 0（仅追问时）：有 conversation_id → 载入近 3 轮历史 → LLM 指代消解（"那市场侧呢"→"春晚市场侧保障人数"）
  → Step 1: LLM 理解意图，生成搜索策略（精确关键词 + 文件过滤）
  → Step 2: 关键词扩展 + ripgrep JSON 搜索 + IDF 覆盖度文件排序 + 统计 section 优先摘录
  → Step 3: LLM 基于搜索结果流式生成回答（全局视角，先总体后明细；追问时注入对话历史保持连贯）
  → 返回回答 + 引用文档链接（最多5个）+ 耗时分析
  → 自动保存到聊天历史（按 conversation_id 归组）
```

> 多轮对话为**旁路设计**：新会话首轮无历史 → 跳过 Step 0 → 与单问字节级一致，离线 golden set 指标无劣化。设计详见 [§10 多轮对话上下文设计](#10-多轮对话上下文设计)。

### 2.3 搜索引擎架构

```
用户问题 → LLM 生成 keywords[]（最小核心实体词，年份单列）
  → 关键词扩展（去尾变体 + 4字拆分 + 数字单位前缀剥离，过滤短数字）
  → ripgrep --json 逐词搜索（max-count=200/文件），记录每词命中文件数（DF）
  → 文件打分：Σ IDF(文件命中的不同关键词) × (1 + 文件名命中加权)
  → 取分数最高的 10 个文件
  → 上下文摘录：统计/总计 section 50% 预算优先 + 超预算截取不跳过
  → 60K 字符上下文 → LLM 回答
```

> 搜索方案的完整设计与离线评测见 [§9 搜索方案详细设计](#9-搜索方案详细设计)。

### 2.4 数据流

**文件上传流程：**
用户上传文件 → 解析（docx/xlsx/pptx/pdf/md/txt）→ 提取 @人名 → 存储 Markdown → 记录元数据 + 导入历史

**链接导入流程：**
用户输入 URL → Playwright 抓取 → protobuf 解码 → 保留链接/图片 → 递归子文档（最多3层）→ SSE 实时推送进度 → 失败记录持久化

**查询流程：**
用户提问 → LLM 搜索策略 → ripgrep 搜索 → 文件选择 + 摘录 → LLM 回答 → 引用文档 → 保存历史

---

## 3. 项目文件目录

```
C:/Code/Qlipoth/
├── backend/
│   ├── main.py                 # FastAPI 入口，CORS，路由注册
│   ├── auth.py                 # 用户认证（注册/登录/角色/Token）
│   ├── config.py               # 配置管理
│   ├── config.json             # 运行时配置文件
│   ├── database.py             # SQLite 初始化（documents/import_trees/users/chat_history/failed_imports）+ 幂等迁移
│   ├── parsers.py              # 文档解析器（docx/xlsx/pptx/pdf+OCR/md/txt）
│   ├── searcher.py             # 搜索引擎（ripgrep JSON + 关键词扩展 + fallback）
│   ├── scraper.py              # 在线文档抓取（Playwright + protobuf + 递归 + 链接保留）
│   ├── llm.py                  # LLM 调用（双格式 + 流式 + 人设注入 + 搜索策略/指代消解/历史回灌 prompt）
│   ├── Soul.md                 # 人设定义（活动保障知识库助手）
│   ├── requirements.txt        # Python 依赖
│   ├── metadata.db             # SQLite 数据库
│   ├── .browser_data/          # Playwright 浏览器持久化数据
│   ├── knowledge_base/         # 知识库文档存储
│   ├── eval/                   # 搜索离线评测体系
│   │   ├── golden_set.json     # 评测集（100 题 / 50 文档，分 5 类）
│   │   ├── generate_questions.py  # 基于知识库批量生成评测问题
│   │   ├── run_eval.py         # 复用生产搜索链路，计算 Recall@K/MRR/Precision
│   │   └── reports/            # 评测报告（按 tag + 时间戳，md + json）
│   └── routes/
│       ├── upload.py           # 文件上传 + 链接导入（SSE 流式）+ 失败记录 + 重试
│       ├── query.py            # 查询接口（SSE 流式）+ IDF 覆盖度文件排序 + 多轮上下文（指代消解/历史滑窗）+ 聊天历史/会话 CRUD
│       ├── documents.py        # 文档管理 + Markdown 渲染查看 + 设置
│       └── openspec.py         # OpenSpec 看板接口（admin 专属，解析 specs/changes）
├── frontend/
│   ├── src/
│   │   ├── App.vue             # 根组件（登录/注册 + 角色导航 + 用户信息）
│   │   ├── router.js           # 路由配置
│   │   ├── api/index.js        # API 封装（REST + SSE + 认证 + 会话历史）
│   │   ├── store/profiling.js  # 性能分析 store
│   │   ├── utils/markdown.js   # Markdown 渲染（marked + DOMPurify 净化）
│   │   └── views/
│   │       ├── Chat.vue        # 智能问答（会话分组侧栏 + 主聊天区 + 追问续聊）
│   │       ├── Upload.vue      # 上传文档（文件/链接/失败重试 Tab）
│   │       ├── Documents.vue   # 文档管理 + Markdown 富文本预览
│   │       ├── Users.vue       # 用户管理（管理员专属）
│   │       ├── Profiling.vue   # 性能分析
│   │       ├── OpenSpec.vue    # OpenSpec 规范看板（管理员专属）
│   │       └── Settings.vue    # LLM 设置
│   ├── index.html
│   ├── package.json
│   └── vite.config.js
├── openspec/                   # 规范驱动开发目录
│   ├── specs/                  # 已实现能力规范（agentic-search/chat-history/multi-turn-context 等）
│   └── changes/                # 变更提案（proposal/design/spec/tasks），archive/ 存归档
├── start.ps1                   # PowerShell 一键启动
└── README.md                   # 本文档
```

---

## 4. 核心功能模块

### 4.1 文档上传与解析

支持格式：.docx, .xlsx, .xls, .pptx, .pdf, .md, .txt

解析策略：
- **Word (.docx)**：提取段落文本、标题层级、表格内容，转为 Markdown
- **Excel (.xlsx)**：按 Sheet 提取，每个 Sheet 转为 Markdown 表格，宽表自动拆分
- **PPT (.pptx)**：按 Slide 提取文本框内容
- **PDF (.pdf)**：优先提取文本层；图片型 PDF 自动 OCR（Tesseract，支持中英文）
- **Markdown/TXT**：直接存储

附加处理：
- 自动提取 `@人名` 作为负责人元数据
- 文件去重（同名文件不重复导入）
- 导入历史持久化

### 4.2 在线文档链接导入

支持企业微信文档（doc.weixin.qq.com）和腾讯文档（docs.qq.com）。

**技术方案：**
- Playwright 启动系统 Chrome（复用用户登录态）
- 网络拦截捕获 `dop-api/opendoc`（doc）和 `dop-api/get/sheet`（sheet）接口响应
- protobuf 解码提取文本，保留 HYPERLINK 转为 Markdown 链接格式
- 递归抓取嵌套文档（用户可控 0-3 层），SSE 实时推送进度
- 父文档已导入不阻断，子文档照常录入
- 失败记录持久化，支持后续重试
- 导入历史记录完整父子关系和源链接

### 4.3 Agentic Search 查询

**搜索引擎（ripgrep）：**
- 关键词扩展：3+字中文加去尾变体（覆盖率→覆盖），4字拆半，数字+单位前缀剥离（26年春晚→春晚），过滤短数字
- IDF 覆盖度排序：按"文件命中的不同关键词"累加 IDF，命中稀有词一次胜过命中宽泛词多次
- 文件名加权：文件名含原始关键词时整体加权，稳住"文件名即强信号"
- 上下文摘录：统计/总计 section 50% 预算优先，超预算截取不跳过
- 每关键词 max-count=200，避免大文件噪音

> 排序算法的演进、踩坑与离线评测数据见 [§9 搜索方案详细设计](#9-搜索方案详细设计)。

**LLM 回答：**
- 人设：大型活动保障知识库助手，全局视角
- 先总体后明细，不局限于某个部门
- 引用文档：最多5个，在线链接可跳转，本地文档可打开 Markdown 渲染页

### 4.4 用户体系与权限

**三级角色：**
- **admin（超级管理员）**：所有功能 + 用户管理
- **super（超级用户）**：智能问答 + 上传文档 + 文档管理 + 查看所有人问答历史
- **user（普通用户）**：智能问答 + 只看自己的问答历史

**认证机制：**
- 用户名 + 密码注册/登录
- 密码要求：8位以上，含大小写字母、数字、特殊字符
- 数据库存储 PBKDF2-SHA256 哈希 + 随机盐
- Cookie Token 自动登录（7天有效）
- 未登录无法使用系统

### 4.5 聊天历史与多轮对话

- 每次问答自动保存（问题 + 回答 + 引用文档 + 时间 + 用户 + conversation_id）
- Chat 页面左侧栏按**会话分组**展示，显示末轮问题、轮次数、时间、用户，按时间倒序
- 点击会话载入全部轮次（user/assistant 交替回显），可在原会话继续追问
- 追问时自动指代消解 + 历史回灌，理解"那市场侧呢""这个再细化下"式省略
- admin/super 可看所有人历史，普通用户只看自己的
- 历史无 conversation_id 的旧记录以单轮伪会话（`legacy-{id}`）兼容渲染

### 4.6 OpenSpec 规范看板（管理员专属）

- 规范驱动开发：能力规范沉淀于 `openspec/specs/`，变更提案位于 `openspec/changes/`
- 看板展示已实现能力（Purpose + Requirement/Scenario）与待办提案（Why/What Changes + 任务进度）
- 变更通过 CLI 归档（`openspec archive`）后自动生成 spec，代码与文档同源演进

### 4.7 失败重试

- 递归抓取中失败的文档自动记录到 `failed_imports` 表
- 上传文档页「失败重试」Tab（管理员可见）
- 支持单选/全选批量重试
- 成功或因重复跳过的自动从失败列表移除

---

## 5. v20260526.1 新增 Feature

| Feature | 说明 |
|---------|------|
| ripgrep 搜索引擎 | 替换原生 grep，JSON 模式输出，速度提升 2-5x |
| priority 文件选择 | 精确中文关键词命中的文件保证入选上下文 |
| 统计 section 优先 | 大文件中"统计/总计"标题的段落优先包含 |
| PDF 支持 | pypdf 文本提取 + Tesseract OCR（图片型 PDF） |
| 用户注册/登录 | 用户名+密码，PBKDF2 哈希，Cookie 7天自动登录 |
| 三级权限 | admin/super/user，导航和功能按角色隔离 |
| 用户管理页 | 管理员查看所有用户、修改角色、删除用户 |
| 聊天历史 | 自动保存问答，左侧栏展示，按角色控制可见范围 |
| 失败记录持久化 | 抓取失败的文档 URL 持久化，支持批量重试 |
| UI 重构 | 左侧历史栏 + 主聊天区布局，登录页居中 |
| 引用文档增强 | 在线链接蓝色跳转，本地文档绿色打开 Markdown 渲染页 |
| @人名 Owner 提取 | 文档中 @人名 自动提取为负责人元数据 |
| 递归层数用户可控 | 0-3 层，输入框选择 |
| 北京时间统一 | 所有时间戳显式使用 UTC+8 |

### v20260603.1 新增 Feature

| Feature | 说明 |
|---------|------|
| IDF 覆盖度排序 | 按文件命中的不同关键词累加 IDF，修正旧版被巨型文档统治的问题 |
| 分词器修复 | 数字+单位前缀复合词剥离（26年春晚→春晚），恢复核心实体召回 |
| 搜索策略 prompt 优化 | 强制拆最小核心实体词、年份单列，提升关键词质量与稳定性 |
| 离线评测体系 | golden set（100 题/50 文档）+ Recall@K/MRR/Precision，复用生产搜索链路 |
| Claude 模型接入 | Anthropic Messages API 双格式兼容，新增 DeepSeek vs Claude 评测对比 |

### v20260604.1 新增 Feature

| Feature | 说明 |
|---------|------|
| 多轮对话上下文 | 追问指代消解（"那市场侧呢"→补全为完整问题）+ 历史回灌，旁路设计不动搜索内核 |
| 指代消解旁路 | 仅有上文时触发独立廉价 LLM 调用，新会话首轮零开销，单问 golden set 指标无劣化 |
| 历史滑窗与预算 | 近 3 轮滑窗 + 独立 6000 字预算（与搜索 60K 互不侵占），超限先丢最旧轮、截断标记 |
| 会话分组侧栏 | 聊天历史按 conversation_id 归组，显示轮次数，可载入续聊；旧记录 legacy 兼容 |
| OpenSpec 看板 | 管理员专属规范驱动开发面板，展示 specs 能力规范与 changes 变更提案进度 |
| Markdown 富文本渲染 | marked + DOMPurify 安全渲染，聊天回答与本地文档预览支持富文本，防 XSS/标签劫持 |

### v20260605.1 新增 Feature

| Feature | 说明 |
|---------|------|
| 文档分类与标签 | 扁平多标签体系，手动打标 + 内容解析自动打标（两阶段：主题标签优先，运营标签兜底）；文档管理页按标签 OR 筛选，上传/导入页可指定标签 |
| 自动打标补录 | 存量未打标文档基于内容批量自动打标，优先复用现有标签，无匹配时基于内容拟定新标签 |
| 查询按标签过滤 | 查询接口新增可选 tag 过滤，搜索前将候选文件限定到带该标签集合再走既有链路；不带 tag 时与现状字节一致，零 golden set 回归 |
| BM25 旁路重排 | 零依赖 char-bigram 分词 BM25（Okapi k1=1.5/b=0.75）+ RRF 融合（k=60）现网 grep 排序，Recall@10 0.71→0.83、MRR 0.44→0.54；异常自动回退原排序，缺省零回归 |
| 知识库方案调研 | gbrain/Khoj 评估（气隙环境 embedding 不可用，不适配）；A/B 实测 char-bigram 优于 jieba 词级（0.78 vs 0.75），生产零新增运行时依赖 |
| 流式空补全兜底 | 流式回答空补全三级兜底，避免模型偶发空输出导致前端空白 |
| 多用户多轮模拟评测 | 模拟user_a/Duck/剑剑多用户随机提问 + 追问，端到端评估搜索效率与回答有效性 |

### v20260605.2 新增 Feature

| Feature | 说明 |
|---------|------|
| 图文回答（相关图片画廊） | 提问后在文字答案下方展示「相关图片」缩略图画廊：从检索选出的 top 来源文档正文抽取干净内容图（图床白名单 + 类型/扩展名边界，避开头像与 protobuf 乱码），去重、单篇限额保证跨文档多样性。文件级相关性（图与答案同源文档），气隙无 embedding 下的可行方案。纯旁路 meta 下发，不改 search_text/排序/prompt，零检索回归 |
| 文档查看器图片渲染 | 文档管理「查看」时，正文裸 URL 内容图自动提取并以「文档图片」区块渲染为图片而非链接 |
| 标签全站配色统一 | 抽出共享配色工具，首页与文档管理页标签共用同一份按标签名语义代表色（每标签不同色），未知标签名哈希稳定取色 |

### v20260605.3 新增 Feature

| Feature | 说明 |
|---------|------|
| BM25 长度归一调参 | 生产 BM25 长度归一参数 `b` 由教科书默认 0.75 调到 1.0（完全长度归一）。本库 628 篇、`avgdl≈1 万 token`、巨型扁平导入文档多，`b=0.75` 下长文档凭词频堆叠刷分挤掉相关短文档。经 `eval/bm25_tune.py` 在 100 题 golden set 网格调参确认最优：纯 BM25 Recall@10 0.79→0.85 / MRR 0.54→0.60，与 grep 的 RRF 融合后 0.86→0.87，分类型无任一回退（排查 0.94 / 歧义 1.0 / 负责人 0.89→0.95）。零依赖、纯参数改动、不动检索结构，可秒级回滚 |

### v20260605.4 新增 Feature

| Feature | 说明 |
|---------|------|
| 问题分类路由（回答模型分级） | 按问题类型把回答生成路由到不同成本的模型：质量敏感的负责人/方案/歧义类走强模型 opus 保质量，数量/排查类走快模型 sonnet 提速。分类用零延迟规则分类器（golden set 5 类准确率 85%、路由二分 90%，与 haiku 等效但不耗 LLM）。100 题端到端横评：完整耗时较全 opus -42%（19.5→11.3s）、质量 4.45≈全 opus 4.58（优于全 sonnet 4.29）、opus 调用占比降到 31%。关键：负责人类经路由回 opus，质量从 sonnet 的 4.15 拉回 4.46，补上纯换模型方案的短板。配置 `llm_model_fast` 留空即整体回退强模型、零回归可秒级回滚；检索链路完全不动 |

### v20260605.5 新增 Feature

| Feature | 说明 |
|---------|------|
| 导入历史结果可视 | 修复递归导入历史的展示缺陷：① 根文档标题抓取失败落库为 `"未知"` 时，回退到树中首个有标题的成功节点（仍无则回退根 URL），不再原样显示"未知"；② 历史条目原来只显示成功文档数，现并列展示「成功/跳过/失败」三类计数（非零才显示失败、跳过标签），一批导入有多少失败一眼可见。`GET /upload/trees` 返回新增 `counts` 与 `display_title`，从 tree_data 实时计算，旧记录同样受益，不改导入/去重/存储逻辑 |
| 失败导入批量重试 | 对 `failed_imports` 中因超时/未取到数据等临时性失败的记录批量重试（复用登录态浏览器上下文），成功则落盘入库并清除失败记录，权限类失败保留标注 |

### v20260608.1 新增 Feature

| Feature | 说明 |
|---------|------|
| Golden Set 升级至150题 | 文档量增至688篇、功能演进后重建评测集：原100题基础上定向补题（S15赛事活动期从0→34题、薄弱标签成本/接口与配置/模板与名单、薄弱类型方案/歧义），每题回填 `tags`+`era` 元数据，新题经 LLM 质检剔除"答案不在来源文档"的条目。最终三维均衡（类型/活动期/标签全覆盖）。当前服务基准：Recall@10 0.873 / MRR 0.584 / 0错误。出题脚本支持按活动期/标签/类型定向抽样 |
| 评测多维分组 | `run_eval.py` 检索链路对齐生产（含 BM25-RRF 融合），报告新增按活动期、按标签分组指标，定位维度盲区 |
| 标签过滤搜索评测 | 模拟"用户点击标签后搜索"，复用生产 tag_ids 路径对比无过滤。结论为纯收益：候选从5925→2312篇、Recall 0.86→0.93、MRR 0.59→0.70，而标签查询单次仅1.5ms几乎零开销 |
| 功能性测试 | 标准库 unittest 零依赖（气隙可跑），15例覆盖问题路由分类/导入历史计数与标题兜底/BM25参数与融合回退/标签过滤裁剪。运行 `py -3.12 -m unittest eval.test_features` |

### v20260608.2 新增 Feature

| Feature | 说明 |
|---------|------|
| 活动维度标签 | 在10个主题标签(弹幕/红包/高可用…)之外，新增 **活动维度** 标签：S赛/跨晚/春晚，标识文档服务的大型活动。两维度正交——一篇文档可同时有主题标签+活动标签，且活动标签可多选(CNY项目常同时含跨年晚会与除夕春晚，则跨晚+春晚都打)。规则+LLM混合全库回填688篇：文件名命中明确活动词走规则，CNY笼统词/无关键词/需细分的走LLM读正文判断。分布春晚561/跨晚253/S赛67，94%文档有活动标签。前端：活动标签专属醒目配色(S赛品红/跨晚深紫/春晚喜庆红)，首页检索筛选与文档管理页活动标签分组置前 |

### v20260608.3 新增 Feature

| Feature | 说明 |
|---------|------|
| 保障清单（历史踩坑预警） | 克里珀从"问答型知识库"升级为"作战记忆体"的第一步。备战开工时选活动类型(S赛/跨晚/春晚)，系统扫该活动的历史**复盘类文档**，用 LLM 按**保障六维度**(高可用保障/直播播放体验/安全/业务需求/成本/可复用沉淀)提炼上届踩过的坑，每条含 现象/原因/当时处置/给这届的建议/该提前到的时点/来源文档，落库成一份按维度分组、可勾选「已处理」、可编辑增删、可跟踪进度的备战清单。把老员工脑里的隐性经验变成新人可直接调用的显性清单。独立导航 TAB「保障清单」，不耦合现有页面。生成为后台任务、前端轮询进度。第一版聚焦复盘文档(信噪比最高)。验证：S赛 43 篇复盘抽出 117 条踩坑，现象具体带数据、建议可落地、成本维度准确抓到"采购应前置"、来源可追溯 |

### v20260609.1 新增 Feature

| Feature | 说明 |
|---------|------|
| 保障清单结构升级 | ①维度由7→6，撤掉过虚的"可复用沉淀"(内容并入其余维度)；②新增「事故/故障」维度并置顶警醒，全库扫描82篇打上"事故/故障"文档标签；③每条加 P0/P1/P2 严重度分级，前端默认展开P0+P1、折叠P2，可按严重度筛选；④抽取源扩为"全文档+安全标签文档"(不再只复盘)，并做**跨活动经验借鉴**(S赛的坑借鉴春晚/跨晚的通用经验，反之亦然)，跨活动条目去重+每维限额10条防灌水；⑤来源改为可点击的**原始企业微信文档链接**(反查 source_url) |
| smartsheet 新版解析器 | 腾讯智能表格新版解析：①URL 的 tab 参数才是真数据表 subId，主动定位拉取(修复前端默认加载空子表、及 URL 清洗误删 tab 参数两个bug)；②解析新版嵌套结构(列名在字段定义 k30、单元格按文本/单选/日期/数字分类型还原)，输出 markdown 表格；老版 blob 逻辑保留兜底。实测完整抓取 55行15列的项目管理表 |
| 自动打标统一 | 上传时自动打标与离线复核统一为同口径：单次 LLM 判定 10 个主题标签 + "事故/故障"(从严)，去掉割裂的"7+3两段式"；手动标签(活动标签)与自动主题标签合并生效；URL 递归导入时活动标签自动传递给所有子文档(修复子文档拿不到活动标签的bug)。上传即一步到位，不必事后补扫 |

---

## 6. 重点需求列表

| 优先级 | 需求 | 状态 |
|--------|------|------|
| P0 | 文件上传与格式转换（含 PDF OCR） | 已完成 |
| P0 | Agentic Search 查询（ripgrep + priority） | 已完成 |
| P0 | SSE 流式输出 | 已完成 |
| P0 | 多模型兼容（OpenAI/Anthropic 双格式） | 已完成 |
| P0 | 用户注册/登录/权限体系 | 已完成 |
| P0 | 搜索排序优化 + 离线评测体系 | 已完成 |
| P0 | 人设体系（全局视角，先总体后明细） | 已完成 |
| P0 | 在线文档链接导入（递归 + SSE 进度） | 已完成 |
| P0 | 聊天历史持久化 | 已完成 |
| P1 | 多轮对话上下文（指代消解 + 历史回灌） | 已完成 |
| P1 | 失败记录持久化 + 批量重试 | 已完成 |
| P1 | 用户管理（角色修改/删除） | 已完成 |
| P1 | @人名 Owner 提取 | 已完成 |
| P1 | 引用文档链接（在线+本地） | 已完成 |
| P1 | 导入历史（父子关系 + 源链接） | 已完成 |
| P1 | Markdown 富文本渲染（回答 + 文档预览） | 已完成 |
| P1 | OpenSpec 规范驱动开发看板 | 已完成 |
| P2 | 企微扫码登录 | 待开发（需企微应用权限） |
| P2 | 文档分类与标签（手动+自动打标、按标签筛选/查询过滤） | 已完成 |
| P2 | 图文回答（相关图片画廊，答案来源文档抽图旁路下发） | 已完成 |

---
<!-- PLACEHOLDER_DEPLOY -->

## 7. 部署与运行

**环境要求：**
- Python 3.12+
- Node.js 18+
- ripgrep（`winget install BurntSushi.ripgrep.MSVC`）
- Tesseract OCR（`winget install UB-Mannheim.TesseractOCR`）+ 中文语言包
- Google Chrome（Playwright 抓取用）

**一键启动（PowerShell）：**
```powershell
.\start.ps1
```

**手动启动：**

后端：
```bash
cd backend
pip install -r requirements.txt
playwright install chromium  # 首次需要安装浏览器
python main.py
```

前端：
```bash
cd frontend
npm install
npm run dev
```

**访问地址：** http://localhost:3000

**首次使用：**
1. 用预置管理员账号登录
2. 在"设置"页配置 LLM API 信息
3. 链接导入首次使用时会弹出 Chrome 窗口，需完成企业微信登录

---

## 8. 性能基准（DeepSeek-v4-pro，2026-05-26）

| 指标 | 数值 |
|------|------|
| 平均总耗时 | ~20s |
| 搜索策略 LLM | ~7s |
| ripgrep 搜索 + 摘录 | ~0.1s |
| 回答生成 LLM（流式） | ~12s |
| 首字输出延迟 | ~8s（策略 LLM + 搜索后即开始流式） |

**优化点**：ripgrep 替换 grep 后搜索阶段从 0.4s 降到 0.1s；IDF 覆盖度排序确保稀有关键词命中的文件不被巨型文档淹没；统计 section 优先确保数量问题能拿到汇总数据。

---

## 9. 搜索方案详细设计

### 9.1 为什么不用向量检索

传统 RAG 需要 embedding 模型 + 向量数据库，引入额外基础设施、切块策略调参与索引重建成本，且对"@人名负责人""统计总计"这类结构化、数值型问题召回不稳。克里珀改用 Agentic Search：把"理解意图 → 决定搜什么 → 整合答案"交给 LLM，把"在全库精确定位文本"交给 ripgrep。知识库纯文本落盘，新增文档零索引成本，问题可解释、可调试。

### 9.2 搜索链路四段

```
LLM 搜索策略 → 关键词扩展 → ripgrep 逐词搜索 → 文件打分排序 → section 摘录 → LLM 回答
```

**① LLM 搜索策略（llm.py: SEARCH_STRATEGY_PROMPT）**

LLM 输出 `keywords[]` + `file_pattern` + `need_full_file`。Prompt 的关键约束：
- 拆最小核心实体词，不带年份/序数前缀（"26年春晚"→"春晚"）。长复合词会让召回率骤降。
- 涉及年份/同比/对比时，把年份单独列为关键词（2025、2026），因为数据常与年份并列。
- 数量问题（"多少""几个""人数"）补充"总计/统计/合计"等汇总词。
- "谁负责 XX"类问题，结果中 @ 后的人名即负责人。

**② 关键词扩展（searcher.py: `_expand_keywords`）**

ripgrep 是字面匹配，中文无分词，故需人工扩展：
- 3+ 字中文加去尾变体：覆盖率 → 覆盖
- 4 字中文拆两半：版本覆盖 → 版本、覆盖
- 数字+单位前缀剥离：26年春晚 → 春晚（剥离"年"），25届春晚 → 春晚（单位字集合 `年月日届期季版号周次轮`）
- 过滤 ≤2 位纯数字，避免噪音

> 分词器踩坑：早期版本对 `26年春晚` 只产出 `['26年春晚','年春']`，核心词"春晚"被时间前缀粘连而丢失，导致该类问题持续答错。修复后产出 `['26年春晚','春晚','年春']`，核心实体得以召回。

**③ ripgrep 逐词搜索（searcher.py: `_rg_search` / `grep_search`）**

每个扩展词跑一次 `rg --json --ignore-case --max-count 200`。max-count 限制单文件命中数，避免巨型文档刷屏。搜索时记录每个词命中的不同文件数（DF, document frequency），供 IDF 使用。ripgrep 不可用时回退纯 Python 遍历（`_fallback_search`）。

**④ 文件打分排序（query.py: `_select_files`）**

这是两波优化的核心。打分公式：

```
score(file) = Σ_kw IDF(kw) × (1 + filename_hit(file))
其中 kw 遍历"该文件命中的不同关键词"
IDF(kw) = log((N+1)/(DF(kw)+1)) + 0.5    # N 为知识库 .md 总数（60s 缓存）
filename_hit：文件名含原始关键词 → +2.0；含去尾变体 → +0.8
```

关键设计：**按"文件命中的不同关键词"累加 IDF，而非按命中行累加**。命中稀有词（如"版本覆盖率"，全库仅 3 篇）一次，远胜命中宽泛词（如"春晚"，263 篇）五十次。这正是修正旧版"按命中行累加被巨型文档统治"的要害。

### 9.3 排序算法演进（两波优化）

| 阶段 | 排序策略 | Recall@10 | MRR |
|------|----------|-----------|-----|
| baseline | priority 文件前5 + 命中数排序 | 0.43 | 0.24 |
| wave-1 | 纯 IDF 按命中行累加 | 0.37（回退） | — |
| wave-1b | priority + 文件名加权 | 0.54 | 0.34 |
| wave-2 | **IDF 覆盖度 + 文件名加权** | **0.72** | **0.44** |

> wave-1 的纯 IDF 按命中行累加反而劣化（用固定关键词离线 A/B 验证 0.43→0.37），原因是巨型文档命中行多、累加分高。wave-2 改为按"不同关键词"累加 IDF 后，命中率与 MRR 同步跃升。

### 9.4 离线评测体系（eval/）

为避免"凭感觉调参"，搭建可复现的离线评测：

- **golden_set.json**：100 题 / 50 文档，分排查类、数量类、方案类、歧义类、负责人类 5 种，每题标注期望命中文档。
- **run_eval.py**：直接复用生产链路（`generate_search_strategy` → `grep_search` → `_select_files`），保证评测口径与线上一致。指标含 Recall@K、MRR、Precision@K、全命中率，按类型分组输出。`elapsed` 仅计搜索链路耗时（策略 LLM + ripgrep + 文件选择，不含回答生成），即"搜索效率"。
- **reports/**：每次跑带 tag + 时间戳，落 md + json，便于版本间 A/B 对比。

### 9.5 DeepSeek vs Claude 评测对比（wave-2，同一 golden set）

| 指标 | DeepSeek-v4-pro | Claude（opus-4-8） |
|------|-----------------|-------------------|
| Recall@10 | 0.72 | 0.7245 |
| MRR | 0.4403 | 0.4675 |
| Precision@10 | 0.081 | 0.0768 |
| 搜索链路总耗时 | 205.5s | 236.3s |
| 评测中失败数 | 0 | 2（502 网关，非模型问题） |

结论：
- **召回基本持平**（0.72 vs 0.72），说明召回上限主要由关键词扩展 + 排序决定，而非模型。
- **Claude 排序质量更优**（MRR 0.44→0.47），意图较重的方案类、歧义类提升明显（方案类 Recall 0.6→0.8，歧义类 0.67→0.83），得益于关键词更稳定、更贴合核心实体。
- **DeepSeek 更快且更稳**（耗时低 ~13%，本轮零失败）。Claude 的 2 次失败为 502 网关抖动，属基础设施而非模型能力。
- 选型权衡：追求极致排序质量用 Claude，追求速度/成本/稳定用 DeepSeek，二者召回相当。

---

## 10. 多轮对话上下文设计

### 10.1 设计目标与核心约束

支持"那市场侧呢""这个再细化下"式追问，让系统理解指代与省略。**最高优先级约束：单问 golden set 指标绝不劣化。** 离线评测直接调 `generate_search_strategy(question)`，因此**绝不修改** `generate_search_strategy` / `grep_search` / `_select_files` / `MAX_CONTEXT_CHARS` 驱动的上下文构建。新逻辑全部走旁路，缺省/无 conversation_id 时与单问字节级一致。

### 10.2 旁路架构（上游消解 + 下游回灌）

```
有 conversation_id？
  ├─ 否（新会话首轮）→ 跳过，resolved_q = question，零额外开销 → 走原单问链路
  └─ 是 → 载入近 3 轮历史 → 指代消解（独立 LLM）→ resolved_q
            → generate_search_strategy(resolved_q)  ← 喂消解后问题，下游不变
            → 搜索/摘录（完全不变）
            → stream_answer(resolved_q, search_text, history_block)  ← 注入历史
```

- **上游消解**：`resolve_coreference(history_block, question)` 独立廉价调用（temperature=0），输出 `{is_followup, resolved_question}`。`is_followup=false` 或解析失败 → 回退原问题，下游字节一致。
- **防过度改写**：prompt 明确"仅当含代词/省略/『那…呢』式追问时才补全，自包含问题原样返回"，补全后保持最小实体词，呼应搜索策略的关键词哲学。
- **下游回灌**：`stream_answer` 加 `history_block` 参数。空 → 用原 `ANSWER_PROMPT`（回归安全）；非空 → 用 `ANSWER_PROMPT_WITH_HISTORY`，以单条文本块注入历史，仅供语气/连贯，不改消息结构。
- **为何独立而非折进策略生成**：折进会污染调优过的策略 prompt、有 standalone 召回回归风险。独立 = golden 路径可证明不变，代价仅追问时一次额外串行往返，首轮零成本。

### 10.3 历史滑窗与预算

- 轮次排序用 `id ASC`（`created_at` 秒级字符串，同会话可能同秒碰撞）。
- 滑窗取最近 **N=3 轮**（一轮 = 一对 Q+A），独立预算 `MAX_HISTORY_CHARS=6000`（**不**从搜索的 60K 里切，避免改动 `per_file_budget` 数学）。
- `_build_history_block`：超预算先丢最旧轮；单条最新轮仍超则截断答案加 `…(截断)`。纯函数可单测。

### 10.4 数据与接口

- `chat_history` 加 `conversation_id TEXT`（可空，幂等迁移），索引 `idx_chat_conv(conversation_id, id)`。NULL 旧行 = 单轮 standalone，回归安全。
- **id 由前端生成**（`crypto.randomUUID()`），首次发送前生成，stream 与 save 请求复用同一个，后端只存不透明字符串。
- 新增 `GET /chat/conversations`（会话分组列表，NULL 行以 `legacy-{id}` 伪会话返回）与 `GET /chat/conversation/{id}`（单会话全部轮次，`id ASC`）。
- meta 事件附带 `original_question` / `resolved_question` / `is_followup` / `timing_coref` 便于调试。

### 10.5 回归验证

- 改动前后跑同一 golden set（`--tag pre` / `--tag post`）。`generate_search_strategy` 等未动 → 指标变化应仅来自 LLM 非确定性（opus-4-8 temperature=0 仍非确定）。三次同代码复跑的方差（Δ≈0.02）覆盖 pre/post 差异，确认无系统性劣化。
- 断言 `_build_history_block([]) == ""` 走未改分支；无 conversation_id 请求的 search_text 与回答 prompt 与单问字节一致。

---

## 11. BM25 旁路重排设计（检索增强）

### 11.1 为什么不上向量检索（gbrain / Khoj）

Task 调研了 gbrain 与 Khoj 两个开源知识库方案，结论是**本环境不适用**：两者检索内核都是句向量 embedding + 向量库语义召回。本机为**气隙环境**——无外网、内网网关只提供 chat completion，无 embedding endpoint（实测 404），embedding 模型 / 向量索引在运行时拉不下来也跑不起来。强行接入只增加部署脆弱性，不解决问题。

替代方案：在现网 ripgrep 词法检索之上，叠加**零依赖 BM25（中文 char-bigram 分词）旁路重排**，与现网召回做 RRF 融合。

### 11.2 设计

```
召回不变：LLM 生成关键词 → ripgrep 全库检索 → _select_files 排序（baseline）
        ↓
旁路重排：进程内零依赖 BM25 对原始问题打分 → bm25_top
        ↓
RRF 融合：RRF([baseline, bm25_top], k=60) → top-10 → LLM
```

- **召回不变**：保留词法精确匹配的高精度（尤其 `@人名`、专有指标）。
- **零依赖 BM25**（`backend/bm25.py`）：Okapi BM25（k1=1.5, b=1.0），char-bigram 分词（"版本覆盖率"→`['版本','本覆','覆盖','盖率','版','本','覆','盖','率']`），纯 Python 实现，气隙机器零新增运行时依赖。长度归一参数 `b=1.0`（完全长度归一）经网格调参确定，见 §11.5。
- **RRF 融合**：`fuse_select` 把 `_select_files` 排序与 BM25 topk 用 Reciprocal Rank Fusion（k=60 等权）合并取 top-10。
- **零回归保证**：BM25 不可用 / 异常 / 空结果时，`fuse_select` 原样返回现网排序；`if results:` 守卫确保无召回时不触发。被 BM25 捞回但无 grep 命中行的文件读开头兜底，不丢召回增益。
- **缓存**：单例索引 + 目录签名（文件数 + 最大 mtime，~4ms/次）失效重建，文档增删改后自动刷新。构建 1.6s / 627 文档 / ~103MB 内存。打分 2-4ms/查询，相对单轮 40s+（LLM 主导）可忽略。

### 11.3 为什么 char-bigram 而非 jieba

A/B 实测：**char-bigram 分词（零依赖）反而优于 jieba 词级分词**（0.78 vs 0.75）。原因是知识库领域词（"版本覆盖率""时移压测""纯净直播间"）大量 OOV，jieba 切错，char-bigram 对专有名词召回更稳。故生产**不引入** jieba / rank_bm25 / numpy。（调研期为对照确曾 `pip install` 跑 jieba 方案，仅评测用，不进生产链路。）

### 11.4 A/B 评测结果（golden_set 100 题，@K=10）

| 方案 | Recall@10 | MRR | Precision@10 | 全命中率 |
|------|-----------|-----|---------------|----------|
| baseline（现网 grep+IDF） | 0.71 | 0.4397 | 0.0752 | 0.71 |
| bm25_char（零依赖，全库 BM25） | 0.78 | 0.5419 | 0.0780 | 0.78 |
| bm25_jieba（jieba 词级） | 0.75 | 0.5028 | 0.0750 | 0.75 |
| **RRF 融合（baseline + bm25_char）← 已落地** | **0.83** | **0.5359** | — | **0.83** |

分类型 Recall@10：排查类 0.75→0.94、数量类 0.59→0.68、方案类 0.80→1.00、歧义类 0.83→1.00、负责人类 0.79 持平（`@人名` 词法匹配本就强，融合不劣化）。

> 生产 `fuse_select` 复算结果与离线 RRF 完全一致（Recall@10=0.8300），确认线上代码路径即评测方案。完整报告见 `backend/eval/reports/TASK5_知识库方案调研与检索增强评测报告.md`，A/B 明细见 `eval/reports/ab_*.md`。

### 11.5 长度归一参数调优（v20260605.3）

BM25 长度归一参数 `b` 此前沿用教科书默认 `0.75`，从未针对本语料调过。本库 628 篇、`avgdl≈1 万 token`、含大量企业微信/腾讯文档导入的巨型扁平文档，`b=0.75` 对长文档惩罚不足——长文档凭累计词频盖过短而精准的目标文档，把后者挤出 top-K。

用 `eval/bm25_tune.py` 在 100 题 golden set 上网格搜索 (k1, b)（复用 `strategy_cache.json`，零 LLM、秒级复跑），确认最优 `b=1.0`（完全长度归一），`k1` 在 0.9–2.6 间对 Recall 不敏感、保持 `1.5`：

| 维度 | b=0.75（旧） | b=1.0（新） |
|------|-------------|------------|
| 纯 BM25 Recall@10 | 0.79 | **0.85** |
| 纯 BM25 MRR | 0.54 | **0.60** |
| 融合后 Recall@10 | 0.86 | **0.87** |
| 融合后 MRR | 0.56 | **0.58** |

分类型（融合后）无任一回退：排查 0.94、数量 0.71、方案 1.0、歧义 1.0、负责人 0.89→0.95。改动仅 `backend/bm25.py` 单一常量 `B`，零依赖、不动检索结构，可秒级回滚。明细见 `backend/eval/reports/bm25_tune_*.md`。

> 注：§11.4 表中融合 0.83 为 v20260605.1 的旧口径（候选内重排变体）；以生产 `fuse_select`（全库 topk ⊕ grep）精确复算，b=0.75 实为 0.86、b=1.0 为 0.87。

---

## 12. 文档分类与标签设计

### 12.1 数据模型

- `tags(id, name UNIQUE)` 标签词表 + `document_tags(document_id, tag_id, PRIMARY KEY 复合)` 关联表，复合主键天然去重。
- 索引 `idx_doctag_tag`、`idx_doctag_doc` 支持双向反查。删文档时级联 `DELETE FROM document_tags`。幂等迁移对齐 `database.py:init_db` 惯例。

### 12.2 打标方式

- **手动**：文档管理页打标对话框（admin），上传/导入页可指定标签。
- **自动（两阶段）**：用户未指定时，解析内容后自动打标——主题标签优先（基于文档内容），运营标签兜底；优先复用现有标签，无匹配时基于内容拟定新标签。存量未打标文档支持批量自动补录。

### 12.3 筛选与查询过滤

- **管理页筛选**：文档列表一次性附带 `tags` 数组，客户端按标签 OR（具备任一所选标签即命中）筛选，与 filterText 取交集。
- **查询过滤（旁路）**：查询接口新增可选 `tag_ids`，搜索前将候选文件限定到带该标签集合（`_tagged_stored_paths` → `_filter_results_by_paths`）再走既有链路。不带 tag 时与现状字节一致，**零 golden set 回归**——沿用多轮对话那套「旁路、缺省零影响」原则。

### 12.4 权限

标签写操作（建/重命名/删标签、给文档打标/去标）仅 admin；读（列标签、看文档标签、按标签筛选）随文档管理页开放给 super+admin。

---

## 13. 图文回答与图片渲染设计

### 13.1 背景：图片为何不可被检索

知识库文档（活动方案/复盘/评审）含大量截图，但图片在数据里是 protobuf 解码残片中的**裸 URL**（非 `![]()` 语法），URL 尾部还带二进制乱码，且**无 caption / 描述文字**。本机气隙、内网网关无 embedding endpoint（实测 404），故无法做图片内容的语义检索。

### 13.2 相关图片画廊（方案 A，旁路零回归）

- **相关性信号 = 文件级关联**：检索链路（IDF 覆盖度 + BM25/RRF）选出的 top 文档，正是文字答案的来源；这些文档里的内容图即与问题同源相关。不用段落级（命中行附近）——实测图片 URL 极少落在摘录窗口 ±20 行内，按片段抽图近乎抽不到。
- **抽取**：`_assemble_context` 从入选文档**整篇正文**抽图，复用与前端 `utils/markdown.js` 对齐的图床白名单正则（qpic.cn 的 `?w=&h=&type=image/` 干净边界、显式图片扩展名），避开头像与乱码；去重、单篇限额（`MAX_IMAGES_PER_FILE=4`）+ 总额限额（`MAX_RELEVANT_IMAGES=8`）保证跨文档多样性。
- **旁路下发**：图片经 `/query` 响应与 `/query/stream` 的 `meta.relevant_images` 字段下发，**不进 `search_text`、不改文件排序与 LLM prompt**。喂给 LLM 的上下文字节级不变，离线评测零影响。
- **前端**：`Chat.vue` 在「引用文档」下方渲染缩略图网格，点击新标签打开原图；`referrerpolicy="no-referrer"` 规避图床防盗链、`loading="lazy"` 懒加载。

### 13.3 文档查看器图片渲染

文档管理「查看」时，`renderMarkdown` 先用同一套图床正则从正文裸 URL 提取内容图，去重后追加「文档图片」区块（转标准 `![]()`）再交 marked 渲染，使查看时看到图片而非链接。仅作用于文档查看器，不影响搜索。
